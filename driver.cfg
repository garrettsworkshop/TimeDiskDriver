MEMORY {
    IOSEL:  file = "bin/iosel.bin",  start = $C000, size = $0800;
    IOSTRB: file = "bin/iostrb.bin", start = $C800, size = $0800;
}
SEGMENTS {
    IOSEL:  load = IOSEL,  type = ro, align = $100;
    IOSTRB: load = IOSTRB, type = ro, align = $100;
}